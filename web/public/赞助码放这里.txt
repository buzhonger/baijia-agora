把你的微信收款码图片放到这个文件夹，命名为：sponsor-qr.png

步骤：
1. 微信 → 我 → 服务 → 收付款 → 二维码收款 → 保存收款码（可设置固定金额或不设）
2. 把保存的图片重命名为 sponsor-qr.png
3. 放进这个文件夹（web/public/）
4. 重新构建（npm run build）或开发模式下刷新即可

界面左下角"❤ 赞助支持"按钮点开后，就会显示这张收款码。
如果没放图片，弹窗会显示一句提示文字，不影响其他功能。
